currencies = (.25, .10)

# Stores values ".25" and ".10" respectively
quarter, dime = currencies

# Can use this idea when creating a for loop
places = [("Colorado Springs", "CO"), ("Pocatello", "ID"), ("Fort Hood", "TX")]
for place, state in places:
    print(place, state)

